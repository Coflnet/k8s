apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mariadb-claim
  namespace: erpnext
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 3Gi
