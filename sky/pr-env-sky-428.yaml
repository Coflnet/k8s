apiVersion: apps/v1
kind: Deployment
metadata:
  name: pr-env-sky-428
  namespace: sky
  labels:
    app: pr-env-sky-428
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pr-env-sky-428
  template:
    metadata:
      labels:
        app: pr-env-sky-428
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
      - name: pr-env-sky-428
        image: registry.flou21.de/sky:428
        imagePullPolicy: Always
        ports:
          - containerPort: 80
        resources:
          requests:
            memory: 100Mi
            cpu: 100m
---
apiVersion: v1
kind: Service
metadata:
  name: pr-env-sky-428-service
  namespace: sky
spec:
  selector:
    app: pr-env-sky-428
  ports:
    - port: 8008
      targetPort: 80
---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: pr-env-sky-428-ingress
  namespace: sky
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: pr-env.flou.dev/sky/428
    http:
      paths:
      - path: /
        backend:
          serviceName: pr-env-sky-428-service
          servicePort: 8008
