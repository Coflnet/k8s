apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: indexer-claim
  namespace: sky
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi
