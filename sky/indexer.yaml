apiVersion: apps/v1
kind: Deployment
metadata:
  name: indexer
  namespace: sky
  labels:
    app: indexer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: indexer
  template:
    metadata:
      labels:
        app: indexer
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
      - name: indexer
        image: registry.flou21.de/sky:af6a5953-16c6-4076-90c4-6aae3437d4cb
        ports:
          - containerPort: 8008
        env:
          - name: DBConnection
            valueFrom:
              secretKeyRef:
                key: connection
                name: secret
          - name: apiKey
            valueFrom:
              secretKeyRef:
                key: api
                name: secret
          - name: redisCon
            value: redis-service
          - name: MODE
            value: indexer
        resources:
          requests:
            memory: "2000Mi"
            cpu: "1000m"
        volumeMounts:
          - mountPath: /data
            name: indexer-mount
            subPath: data
      volumes:
        - name: indexer-mount
          persistentVolumeClaim:
            claimName: indexer-claim-many
---
apiVersion: v1
kind: Service
metadata:
  name: indexer-service
  namespace: sky
spec:
  selector:
    app: indexer
  ports:
    - port: 8008
      targetPort: 8008



---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: indexer-migration
  namespace: sky
  labels:
    app: indexer-migration
spec:
  replicas: 1
  selector:
    matchLabels:
      app: indexer-migration
  template:
    metadata:
      labels:
        app: indexer-migration
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
      - name: indexer-migration
        image: registry.flou21.de/sky:af6a5953-16c6-4076-90c4-6aae3437d4cb
        ports:
          - containerPort: 8008
        env:
          - name: apiKey
            valueFrom:
              secretKeyRef:
                key: api
                name: secret
          - name: DBConnection
            valueFrom:
              secretKeyRef:
                key: connection_live
                name: secret
          - name: MODE
            value: indexer
          - name: stripeKey
            valueFrom:
              secretKeyRef:
                key: stripe_key
          - name: stripeSecret
            valueFrom:
              secretKeyRef:
                key: stripe_secret
                name: secret
        resources:
          requests:
            memory: "3000Mi"
            cpu: "2000m"
        volumeMounts:
          - mountPath: /data
            name: indexer-mount-migration
            subPath: data
      volumes:
        - name: indexer-mount-migration
          persistentVolumeClaim:
            claimName: indexer-claim-migration
---
apiVersion: v1
kind: Service
metadata:
  name: indexer-migration-service
  namespace: sky
spec:
  selector:
    app: indexer-migration
  ports:
    - port: 8008
      targetPort: 8008