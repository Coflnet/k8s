apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: frontend
  namespace: sky
  labels:
    app: frontend
spec:
  replicas: 1
  progressDeadlineSeconds: 2592000
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
      - name: frontend
        image: registry.flou21.de/sky-frontend:0ee11d7f-e5c0-46ea-b434-02f166286ebc
        ports:
          - containerPort: 80
        resources:
          requests:
            memory: "100Mi"
            cpu: "100m"
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 10
          failureThreshold: 5
  strategy:
    blueGreen:
      activeService: frontend
      previewService: frontend-preview
      autoPromotionEnabled: false
      previewReplicaCount: 1
---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: sky
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-preview
  namespace: sky
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80