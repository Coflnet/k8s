apiVersion: apps/v1
kind: Deployment
metadata:
  name: feedback-backend
  namespace: feedback
  labels:
    app: feedback-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: feedback-backend
  template:
    metadata:
      labels:
        app: feedback-backend
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
      - name: feedback-backend
        image: registry.flou21.de/feedback-backend:fca1d18a-0264-4722-90a7-5cfd2eb204ae
        ports:
          - containerPort: 3000
        env: 
        - name: DB_HOST
          valueFrom:
            configMapKeyRef:
              name: config
              key: db_host
        - name: DB_PORT
          valueFrom:
            configMapKeyRef:
              name: config
              key: db_port
        - name: DB_USER
          valueFrom:
            configMapKeyRef:
              name: config
              key: db_user
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              key: db_password
              name: secret
        - name: DB_NAME
          valueFrom:
            configMapKeyRef:
              name: config
              key: db_name
        resources:
          requests:
            memory: "50Mi"
            cpu: "30m"
---
apiVersion: v1
kind: Service
metadata:
  name: feedback-backend
  namespace: feedback
spec:
  selector:
    app: feedback-backend
  ports:
    - port: 3000
      targetPort: 3000
