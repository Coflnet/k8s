apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: elastic-claim
  namespace: elastic
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 15Gi