{{- range .Values.services }}
apiVersion: v1
kind: Service
metadata:
  name: {{ . }}
  labels:
    app: {{ . }}
spec:
  selector:
    app: {{ . }}
  ports:
    {{ toYaml $.Values.ports | nindent 6 }}
---
{{- end }}
